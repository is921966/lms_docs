import XCTest

/// Этот тест предназначен для проверки базовой работоспособности тестовой среды.
/// Он не тестирует какую-либо конкретную функцию, а просто проверяет,
/// что тесты могут быть запущены и успешно завершены.
final class SanityCheckTest: XCTestCase {

    func testInfrastructureIsWorking() throws {
        // Этот тест всегда должен проходить.
        // Если он не проходит, это указывает на проблему в конфигурации проекта,
        // схемах или среде выполнения тестов, а не в коде приложения.
        XCTAssertTrue(true, "The test runner should be able to execute a simple boolean assertion.")
    }

    func testPerformanceExample() throws {
        // Этот тест измеряет производительность пустого блока.
        // Он также должен всегда проходить и используется для проверки
        // того, что измерение производительности работает корректно.
        self.measure {
            // Ничего не делаем, просто измеряем "пустоту".
        }
    }
} 